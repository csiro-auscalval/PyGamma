[scheduler]
record_task_history = true
state_path = <path to store luigi-state.pkl>
retry_delay = 10

[task_history]
db_connection = sqlite:///luigi-task-hist.db

[core]
logging_conf_file = <path to a logging.cfg>

[worker]
keep_alive = True
retry_external_tasks = True
wait-interval = 10
